fn main() {
    println!("{}", 42);
}

// All declarations should appear at the beginning of the method
fn init(a: i32, b: i32) -> i32 {
    let c: i32;
    while (true) {}
    let d: i32;
    return 42;
}
